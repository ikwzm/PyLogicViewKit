PyLogicViewKit
==================================================================================

Overview
----------------------------------------------------------------------------------

A programmable logic waveform viewer toolkit for Python.

PyLogicViewKit is an extensible toolkit for viewing and analyzing logic waveforms generated by digital hardware simulators.

The project currently focuses on waveform data stored in GTKWave FST files and is built on top of Python waveform processing tools.

PyLogicViewKit aims to provide a programmable and extensible environment for waveform visualization, analysis, and future signal processing capabilities.

The project is currently under development and its functionality will continue to expand.

Examples
----------------------------------------------------------------------------------

PyLogicViewKit includes example projects demonstrating how to view and organize logic waveforms.

### AXI4 Master to Stream

The [AXI4 Master to Stream example](examples/nvc/axi4_master_to_stream/) uses an FST waveform generated by an NVC simulation of an AXI4 master-to-stream design.

![AXI4 Master to Stream Example](examples/nvc/axi4_master_to_stream/axi4_m2s_tb_32_32_256_sync.png)

For more information, see the [example README](examples/nvc/axi4_master_to_stream/Readme.md).

Requirements
----------------------------------------------------------------------------------

PyLogicViewKit requires the following software and Python packages:

* Python 3.10 or later
* PySide6
* python-libfst
* C compiler (gcc or clang)

`python-libfst` provides Python bindings for reading and processing GTKWave FST waveform files.

Installation
----------------------------------------------------------------------------------

### From source:

#### 1. Clone the repository:

```console
shell$ git clone --recursive https://github.com/ikwzm/PyLogicViewKit.git
```

#### 2. Setup venv

```console
shell$ python3 -m venv .venv
```

```console
shell$ source .venv/bin/activate
```

#### 3. Install PySide6

```console
shell$ pip install PySide6
```

#### 4. Install python-libfst

```console
shell$ git submodule update --remote --recursive
shell$ pip install ./python-libfst
```

#### 5. Install

```console
shell$ pip install -e .
```

